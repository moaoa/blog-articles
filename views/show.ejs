<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      rel="stylesheet"
      href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css"
      integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh"
      crossorigin="anonymous"
    />
    <title>Document</title>
  </head>
  <body>
    <div class="container">
      <div class="card mt-4">
        <div class="card-body">
          <h1 class="card-title"><%= bayload.article.title %></h1>
          <div class="card-subtitle text-muted">
            <%= bayload.article.createdAt.toLocaleDateString() %>
          </div>
          <div class="card-text "><%= bayload.article.description %></div>
          <div class="card-text ">user : <%= bayload.article.user.name %></div>
          <div class="card-text "><%- bayload.article.markdown %></div>
          <a href="/" class="btn btn-secondary ">All articles</a>

          <% if(bayload.isAtuh) {%>
          <form
            action="/articles/<%=bayload.article.slug%>?_method=DELETE"
            class="d-inline"
            method="POST"
          >
            <button type="submit" class="btn btn-danger ">
              Delete
            </button>
          </form>

          <a
            href="/articles/edit/<%=bayload.article.slug%>"
            class="btn btn-primary "
            >edit</a
          >
          <%}%>
        </div>
      </div>
    </div>
  </body>
</html>
