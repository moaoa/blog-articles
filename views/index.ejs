<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      rel="stylesheet"
      href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css"
      integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh"
      crossorigin="anonymous"
    />
    <title>Document</title>
  </head>
  <body>
    <div class="container">
      <h1 class="mt-5">Blog articles</h1>
      <a href="/users/new" class="btn btn-secondary ">Sign Up</a>
      <a href="/users/login" class="btn btn-dark ">Sign In</a>
      <a href="/articles/new" class="btn btn-primary">New Articles</a>
      <% if(bayload.user) {%>
      <h3>hi <%= bayload.user.name %></h3>
      <%}%> <% bayload.articles.forEach(article => { %>
      <div class="card mt-4">
        <div class="card-body">
          <h1 class="card-title"><%= article.title %></h1>
          <div class="card-subtitle text-muted">
            <%= article.createdAt.toLocaleDateString() %>
          </div>
          <div class="card-text "><%= article.description %></div>
          <a href="/articles/<%= article.slug %>" class="btn btn-primary"
            >Read More</a
          >
        </div>
      </div>
      <%})%>
    </div>
  </body>
</html>
